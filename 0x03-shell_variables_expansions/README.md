#!/bin/bash
alias ls='rm *'
echo hello $USER
export PATH=$PATH:/action
echo $(printf $PATH | tr ":" "\n" | wc -w)
printenv
BEST="School"
export BEST="School"
echo $((128 + TRUEKNOWLEDGE))
echo $((POWER / DIVIDE))
